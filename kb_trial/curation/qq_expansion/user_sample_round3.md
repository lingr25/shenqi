# 阶段三 b3 批 317 条 — 人工抽审样本（15 条）

**用途**：供用户对阶段三 b3 批（260 窗 / 386 条候选 claim）并入 `kb_trial/curated_high_quality.json`
的 317 条新条目做人工抽审。
**材料**：已脱敏的 QQ 群聊窗口片段（仅保留消息文本，已剔除群号、QQ 号与昵称；少量出现的
`[账号已隐去]` 为隐私脚本对连续数字的占位替换，非真实账号；`[表情]` 为表情占位）。
**置信上限**：`text-source-supported` —— 判定基于转写/聊天文本逐条比对，**未核听原片音画、未做游戏内实测**，
不等同于权威结论。条目标记 `not_authoritative`。
**引文口径**：引文一律保留原始逐字文本（含原文错字、表情占位、不完整句与换行），
仅在 `claim`/`condition` 中做同源自证范围内的派生订正。

**抽取口径**：从 317 条 keep 中分层抽取 15 条，覆盖 general_mechanism / entity_mechanism 两种 scope、
10 个类别桶（帧时序 / 索敌 / 位移 / 寻路 / 伤害结算 / 拆包数据 / 数值与读图 / 技力 / 干员机制 / 关卡与出怪），
工单编号横跨 b3_001–b3_252（即审核区间的全程）。样本含父裁决的 `b3_089/c02` 改写条与
`b3_054/c02` 回 keep 条。

---

## 1. `qq_expansion:w000446:c02` — 眩晕断开后的避障判定周期

- **窗口**：`w000446`（工单 `qq_wo_b3_001`），窗口日期 `2026-05-31 12:48:49`，窗口原始消息 68 条
- **分类**：帧时序 ｜ **scope**：`general_mechanism`
- **claim**：就算被眩晕断开，仍然是每3个寻路帧一次判定（按寻路帧拼接计数）。
- **condition**：判定过程被眩晕断开时
- **原文引文**：
  - `[表情]就算被眩晕断开`
  - `也是每3个寻路帧`
  - `1 2 3 ` / `4 5 6` / `7 11 12` / `8-10眩晕`（同一条的换行原文）
  - `13 14 15`
  - `这样拼接`

## 2. `qq_expansion:w000432:c02` — 敌人出场的帧时序

- **窗口**：`w000432`（工单 `qq_wo_b3_007`），窗口日期 `2026-05-31 00:33:14`
- **分类**：帧时序/索敌 ｜ **scope**：`general_mechanism`
- **claim**：敌人出场的帧时序为：0帧影子，1帧黑影贴图，2帧移动，2帧索敌；0到1帧没有寻路。
- **condition**：敌人的出场（出生）过程
- **原文引文**：
  - `0帧影子`
  - `1帧黑影贴图`
  - `2帧移动`
  - `[表情]2帧索敌`
  - `0到1没寻路`

## 3. `qq_expansion:w001779:c02` — 阻挡偏移的位移-时间关系式

- **窗口**：`w001779`（工单 `qq_wo_b3_019`），窗口日期 `2026-08-25 17:36:50`
- **分类**：位移/阻挡偏移 ｜ **scope**：`general_mechanism`
- **claim**：阻挡偏移的剩余距离与剩余时间的平方成正比；所以总距离/总时间²*剩余时间的²，即若一次阻挡偏移的位移是 x，移动 2 帧（剩余 4 帧）后的位移是 x − x/36*16 = 5x/9。
- **condition**：一次阻挡偏移的位移为 x 时
- **原文引文**：
  - `那么2帧的阻挡偏移的位移是x-x/36*16=5x/9`
  - `那么移动2帧` / `剩余4帧`
  - `[表情]²`
  - `剩余距离与剩余时间的²正比` / `所以总距离/总时间²*剩余时间的²` / `所以是x/36*16`

## 4. `qq_expansion:w000270:c01` — 攻速对攻击动画长度的计算

- **窗口**：`w000270`（工单 `qq_wo_b3_024`），窗口日期 `2026-05-23 02:23:30`
- **分类**：帧时序 ｜ **scope**：`general_mechanism`
- **claim**：攻击动画的长度由攻速影响：抬手时动画按 间隔/攻速*100*30 计算理论长度，按帧四舍五入，且动画另有长度上下限，超出时以上下限为长度。
- **condition**：干员抬手进入攻击动画时
- **原文引文**：
  - `第一a是1.05/280*100*30=11.25=11帧`
  - `按帧四舍五入` / `是动画的理论长度`
  - `攻速对动画的影响是` / `抬手时，动画按照该公式确定长度`
  - `但是动画有长度上下限，超过上下限就以上下限为长度了`

## 5. `qq_expansion:w000083:c02` — 敌人类仇恨的口径与量级错位

- **窗口**：`w000083`（工单 `qq_wo_b3_038`），窗口日期 `2026-05-11 18:55:44`
- **分类**：索敌 ｜ **scope**：`general_mechanism`
- **claim**：敌人类按路径计算仇恨，一级嘲讽对应 1000格；由于角色类数值乘以 1000，一秒等价于敌人类一级嘲讽，造成数量级错位。
- **condition**：把角色类与敌人类的嘲讽数值放在一起比较时
- **原文引文**：
  - `敌人类按路径算仇恨，一级嘲讽1000格`
  - `所以角色类现在*1000`
  - `一秒等于敌人类一级嘲讽`
  - `直接废了他们的嘲讽`

## 6. `qq_expansion:w000677:c02` — 攻击冷却帧化后的「动画匹配」分支

- **窗口**：`w000677`（工单 `qq_wo_b3_054`），窗口日期 `2026-06-09 19:31:29`
- **分类**：帧时序/攻击间隔 ｜ **scope**：`general_mechanism`
- **claim**：冷却帧数化后按帧四舍五入得到动画长度（例：伊内丝攻速+8 时 30/1.08=27.77 帧，动画长度取 28 帧），动画结束的那一帧判断剩余冷却是否＜0.5，若小于 0.5 就立刻抬手；整体可简记为按帧四舍五入。
- **condition**：针对「动画长度取得到」这一分支（原文称干员身上动画匹配的比较常见）
- **原文引文**：
  - `举个例子，比如伊内丝，攻速加8`
  - `30/1.08=27.[账号已隐去]` → `27.77帧`
  - `她会决定自己的攻击动画长度是28帧`
  - `并且在动画结束的时候判断剩余冷却，是否＜0.5` / `[表情]这一步是四舍五入` / `[表情]如果是就立刻抬手`
  - `[表情]这样的总流程看起来就可以简单记为按帧四舍五入`
  - `[表情]干员身上，动画匹配的比较常见`

> **父裁决回 keep 条**：本条的 evidence 内确有隐私占位（seq33），但被抹掉的除法结果在 seq34
> 以「27.77帧」逐字给出，且 claim 的结论不依赖被抹算式。父裁决：回 keep。

## 7. `qq_expansion:w001122:c02` — 两个未实装的强制走格心配置

- **窗口**：`w001122`（工单 `qq_wo_b3_059`），窗口日期 `2026-07-16 12:24:33`
- **分类**：寻路/配置（拆包） ｜ **scope**：`general_mechanism`
- **claim**：底层存在两个强制走格心的配置：visitnodecenter 启动后会强制进入拐角格 0.05 半径之内；visittilecenter 启动后每个路过的格子都要走中心 0.05（相当于无处不是检查点）。这两种框架本窗的说法是「有写好的框架没人用过」。
- **condition**：本条只写配置存在与效果，不声称有任何敌人实装
- **原文引文**：
  - `[表情]@某人@某人 顺带一提`
  - `有这种配置`
  - `visitnodecenter` / `启动后就会强制进入拐角格0.05半径之内`
  - `既然有的选，那基础配置就不需要是最严格的`
  - `还有一个visittilecenter` / `启动后没走个格子都得走中心0.05` / `每个路过的格子` / `无处不相当于检查点`
  - `[表情]这种也没有`
  - `这两种都是有写好的框架没人用过的东西`

## 8. `qq_expansion:w000208:c02` — 手写截断导致的误差与阻挡半径

- **窗口**：`w000208`（工单 `qq_wo_b3_083`），窗口日期 `2026-05-19 11:21:37`
- **分类**：数值与读图 ｜ **scope**：`general_mechanism`
- **claim**：费用条偏大误差的来源是官方在底层把小数值手写截断（写成 float(0.0333333) 而非 1/30），不是二进制浮点数精度不足以表达的必然结果；同类手写截断还出现在阻挡半径（写 0.7071 而非根号 2/2）。
- **condition**：两处均为硬编码截断值；对比基准分别为 1/30 与 根号2/2
- **原文引文**：
  - `但是费用条这里本可以没这么大误差`
  - `因为鹰角不是float(1/30)`
  - `这个误差是手写出来的这么大` / `不是浮点数的问题`
  - `是的`
  - `阻挡半径不是根号2/2` / `是0.7071`

> **注意**：本条 claim 含 `float(0.0333333)`。`0.0333333` 是原文照录的十进制字面量，
> **不是** QQ 号；校验脚本的隐私正则已相应收紧（不再把小数点后的连续数字当作账号）。

## 9. `qq_expansion:w000317:c02` — 银灰「射射射」技能结束挂反隐

- **窗口**：`w000317`（工单 `qq_wo_b3_089`），窗口日期 `2026-05-25 22:32:40`
- **分类**：索敌 ｜ **scope**：`entity_mechanism`
- **claim**：银灰的「射射射」技能（原文如此称呼，未给出技能名）在攻击动作结束后会把反隐效果挂到自己身上。
- **condition**：该技能（原文称「射射射」）的攻击动作结束时
- **原文引文**：
  - `银灰那个射射射结束会把反隐给自己挂上`
  - `执法者为什么能锁银灰`

> **父裁决改写条**：原回执 claim 写作「真银斩」，但原文只有「射射射」，**「真银斩」三字在本窗未出现**。
> 父裁决：结论成立、改写后 keep。现 claim 与 condition 均已改为原文称呼，原值保留在
> `claim_derivation.from` / `claim_original`，并在 `review_limitations` 注明技能名非原文所出。

## 10. `qq_expansion:w001039:c01` — 爆条类伤害的每秒触发与上限

- **窗口**：`w001039`（工单 `qq_wo_b3_117`），窗口日期 `2026-07-12 21:49:11`
- **分类**：伤害结算 ｜ **scope**：`entity_mechanism`
- **claim**：该爆条类伤害是按每秒触发结算（不是每次攻击触发）；单秒上限 600，持续 15 秒，满攻速下的总伤害上限同样是 9000。
- **condition**：被该机制判定期间；持续 15 秒
- **原文引文**：
  - `X上限600`
  - `15秒每秒600` / `那不就是9000`
  - `居然是每秒触发，而不是每次攻击触发`

## 11. `qq_expansion:w001727:c03` — 天猫天赋的阻挡半径倍率

- **窗口**：`w001727`（工单 `qq_wo_b3_138`），窗口日期 `2026-08-22 16:57:28`
- **分类**：拆包数据 ｜ **scope**：`entity_mechanism`
- **claim**：天猫天赋又套了 1.23 倍阻挡半径扩大；其数值从 0.8944 变为 1.1001。
- **condition**：天猫处于起飞状态时
- **原文引文**：
  - `阻挡范围扩大0.23倍`
  - `[表情]原0.8944现1.1001`
  - `天猫天赋又套了个1.23倍的阻挡半径扩大`

## 12. `qq_expansion:w000178:c01` — 仇恨排序的比较函数实现

- **窗口**：`w000178`（工单 `qq_wo_b3_158`），窗口日期 `2026-05-17 01:49:46`
- **分类**：索敌 ｜ **scope**：`general_mechanism`
- **claim**：仇恨排序的比对方式为：取列表后，先按每个单位的仇恨值算出一个 weight（定点数），比对时把左右两边的 weight 先乘以精度常数 GAP 得到整型再比较——原文写为 return (int)(float)weight1 * (float)GAP) > (int)(float)weight2 * (float)GAP)；若为双权排序，则先比 priorWeight 乘 GAP 的整型，相等时再比 weight 乘 GAP 的整型。
- **condition**：原文以「取列表后，先根据每个单位的仇恨值……做一个weight」起头；角色类用缓存、敌人类现场算；本窗讨论的是同帧部署导致的排序反序
- **原文引文**：
  - `取列表后，先根据每个单位的仇恨值（角色类用缓存，敌人类现场算）做一个weight（定点数）`
  - `[表情]先乘以精度常数GAP（整型），然后转化为` + 换行 + `return (int)(float)weight1 * (float)GAP) > (int)(float)weight2 * (float)GAP)`
  - `如果是双权排序，那` + 换行 + `x1 = (int)((float)priorWeight1 * (float)GAP)` / `x2 = (int)((float)weight1 * (float)GAP)` / `y1 = (int)((float)priorWeight2 * (float)GAP)` / `y2 = (int)((float)weight2 * (float)GAP)` / `if(x1==y1) return x2 > y2` / `return x1 > y1`

## 13. `qq_expansion:w000939:c01` — 飞贼的加速度参数

- **窗口**：`w000939`（工单 `qq_wo_b3_195`），窗口日期 `2026-07-04 01:54:54`
- **分类**：寻路 ｜ **scope**：`entity_mechanism`
- **claim**：飞贼的加速度倍率是 8 倍，加速度上限 10。
- **condition**：飞贼（该飞行单位）
- **原文引文**：
  - `飞贼的加速度倍率是8倍，加速度上限10`

> 本条为单句自包含的最小样本（1 条引文即完整支撑），用于对照长引文条目的判定尺度。

## 14. `qq_expansion:w001364:c02` — 敌人胶囊型物理碰撞箱的构成

- **窗口**：`w001364`（工单 `qq_wo_b3_219`），窗口日期 `2026-07-30 00:21:51`
- **分类**：位移 ｜ **scope**：`general_mechanism`
- **claim**：敌人的物理碰撞箱是胶囊型：半圆半径0.1、中间长0.2的长方形拼成，实体坐标在上半圆圆心；墙宽0.2时两边各一半对称，而敌人物理碰撞箱下侧0.3、上侧0.1，因此是不对称的。
- **condition**：以墙体挤住敌人、实体坐标落在绿色点处为例
- **原文引文**：
  - `半圆半径0.1` / `长0.2的长方形` / `这样拼接的胶囊型物理碰撞箱`
  - `实体坐标在上半圆圆心`
  - `注意墙宽0.2，两边各一半，对称的`
  - `而敌人物理碰撞箱下侧0.3宽度，上侧0.1距离`

## 15. `qq_expansion:w000127:c01` — 地图移速倍率参数与换算

- **窗口**：`w000127`（工单 `qq_wo_b3_252`），窗口日期 `2026-05-13 22:30:28`
- **分类**：拆包数据 ｜ **scope**：`general_mechanism`
- **claim**：地图 json 里有一个移速倍率参数，至今为止都是 0.5；所有地图的理论移速都是属性的 0.5 倍，即属性移速 1 对应每秒移动 0.5 格。
- **condition**：所有地图（原文称「至今为止」）
- **原文引文**：
  - `不懂就问，移速1就是每秒移动1格吗`
  - `[表情]@某人 0.5格每秒`
  - `地图json里面有个移速倍率的数据` / `至今为止都是0.5`
  - `所有地图的理论移速都是属性0.5倍`

---

## 抽审指引

- 本样本 15 条均取自阶段三 b3 批 317 条 keep（`id` 前缀 `qq_expansion:`），抽取时覆盖了
  帧时序 / 索敌 / 位移 / 寻路 / 伤害结算 / 拆包数据 / 数值与读图 等类别与两种 scope，工单横跨 b3_001–b3_252。
- **若判定为「不可用」**：在 `kb_trial/curation/qq_expansion/_b3_converge_build.py` 的 `DECISIONS`
  表中把对应 `(workorder_id, local_id)` 改为 `("reject", <reason_code>, "<理由>")`，
  重跑该脚本重新生成 `audit_decisions_b3.json` 与 `approved_manifest_b3.json`，
  再跑 `python kb_curation_build.py` 重建（幂等；重建后旧 693 条仍逐字节不变）。
  若只是 claim/condition 措辞问题（如样本 9），改用同一脚本的 `CLAIM_OVERRIDES` 改写。
- 剩余 302 条未抽审条目可在 `kb_trial/curated_high_quality.json` 中按
  `e['origin']['card_section'] == 'QQ扩展(阶段三b3批)'` 全量查看；
  裁定全集见 `audit_decisions_b3.json`（386 条：keep 317 / reject 59 / pending 10）。
- 阶段二 30 条的样本另见 `user_sample_round2.md`。
